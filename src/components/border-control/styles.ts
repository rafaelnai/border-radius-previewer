import { createUseStyles } from "react-jss";

export default createUseStyles({
  control: {
    width: 500,
    height: 500,
    border: "1px solid",
    position: "absolute",
    left: "50%",
    top: "25%",
    transform: "translate(-50%)",
  },
});
